name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  macos:
    permissions:
      attestations: write
      contents: write
      id-token: write
    environment: release
    strategy:
      fail-fast: false
      matrix:
        target:
          - aarch64-apple-darwin
          - x86_64-apple-darwin
    runs-on: macos-14
    env:
      KAVRANTA_TARGET: ${{ matrix.target }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable, 2026-08-23
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          key: ${{ matrix.target }}
      - run: npm ci
      - run: npm run validate:public-boundary
      - run: npm run validate:agent-bundle
      - run: npm run validate:release-version
      - run: npm run validate:release-distribution
      - run: npm run validate:provider-catalog

      - name: Require Apple distribution credentials
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_PRIVATE_KEY: ${{ secrets.APPLE_API_PRIVATE_KEY }}
        run: |
          missing=()
          for name in \
            APPLE_CERTIFICATE \
            APPLE_CERTIFICATE_PASSWORD \
            APPLE_SIGNING_IDENTITY \
            KEYCHAIN_PASSWORD \
            APPLE_API_ISSUER \
            APPLE_API_KEY \
            APPLE_API_PRIVATE_KEY
          do
            if [[ -z "${!name}" ]]; then
              missing+=("$name")
            fi
          done
          if (( ${#missing[@]} > 0 )); then
            echo "Missing required Apple release secrets: ${missing[*]}" >&2
            exit 1
          fi
          if [[ "$APPLE_SIGNING_IDENTITY" != "Developer ID Application:"* ]]; then
            echo "APPLE_SIGNING_IDENTITY must name a Developer ID Application certificate." >&2
            exit 1
          fi

      - name: Import Apple signing certificate
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          certificate_path="$RUNNER_TEMP/env-manager-developer-id.p12"
          keychain_path="$RUNNER_TEMP/env-manager-signing.keychain-db"
          printf '%s' "$APPLE_CERTIFICATE" | base64 --decode > "$certificate_path"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$keychain_path"
          security default-keychain -s "$keychain_path"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$keychain_path"
          security set-keychain-settings -lut 21600 "$keychain_path"
          security import "$certificate_path" -k "$keychain_path" -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$keychain_path"
          if ! security find-identity -v -p codesigning "$keychain_path" | grep -F "$APPLE_SIGNING_IDENTITY" >/dev/null; then
            echo "The imported keychain does not contain APPLE_SIGNING_IDENTITY." >&2
            exit 1
          fi
          rm -f "$certificate_path"
          echo "APPLE_SIGNING_IDENTITY=$APPLE_SIGNING_IDENTITY" >> "$GITHUB_ENV"
          echo "ENV_MANAGER_SIGNING_KEYCHAIN=$keychain_path" >> "$GITHUB_ENV"

      - name: Configure Apple notarization
        env:
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_PRIVATE_KEY: ${{ secrets.APPLE_API_PRIVATE_KEY }}
        run: |
          private_key_path="$RUNNER_TEMP/AuthKey_${APPLE_API_KEY}.p8"
          printf '%s' "$APPLE_API_PRIVATE_KEY" | base64 --decode > "$private_key_path"
          chmod 600 "$private_key_path"
          echo "APPLE_API_ISSUER=$APPLE_API_ISSUER" >> "$GITHUB_ENV"
          echo "APPLE_API_KEY=$APPLE_API_KEY" >> "$GITHUB_ENV"
          echo "APPLE_API_KEY_PATH=$private_key_path" >> "$GITHUB_ENV"
          echo "ENV_MANAGER_NOTARY_KEY_PATH=$private_key_path" >> "$GITHUB_ENV"

      - uses: tauri-apps/tauri-action@1deb371b0cd8bd54025b384f1cd735e725c4060f # action-v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: v__VERSION__
          releaseName: Kavranta v__VERSION__
          releaseBody: |
            Kavranta v__VERSION__ for macOS and Windows.

            - Renamed Env Manager to Kavranta while preserving existing app data and project manifests, and safely migrating legacy agent connections to the Kavranta plugin, Skill, MCP, and Broker identities.
            - Windows 10/11 (x64 beta, unsigned): download the `x64-setup.exe` installer. Microsoft Defender SmartScreen may warn; use only the installer attached to this official release.
            - Apple Silicon: download the aarch64 DMG
            - Intel Mac: download the x86_64 DMG
            - The app opens in English by default and also supports Korean.
            - Connect OpenCode through its global Skill, MCP, and fail-closed plugin interfaces, using the same redacted Broker workflow as the other supported agents.
            - Run locally reviewed Action Packs against a fixed CLI or HTTPS target without returning managed values to the UI, agent, logs, or response body.
            - Let a supported agent install a concretely requested, value-free Action Pack through a redacted plan, then run it as a separate audited action.
            - Verify managed Android App Links certificate fingerprints against explicit public hosts without returning local or remote fingerprints to the agent.
            - Allow source-code patches that only mention env filenames while continuing to block patches whose declared target is an env data file.
            - Pipe a generated value through a five-minute, single-use Broker plan without putting it in command arguments or agent output.
            - Discover Wrangler `.dev.vars*` alongside `.env*`, and surface app or installed AI-integration updates through quiet background checks.
            - Exchange immutable encrypted packages through a mounted NAS or existing sync folder, then review conflicts before applying.
            - Push selected values to AWS Secrets Manager or SSM Parameter Store with optional KMS keys.
            - Compare supported AWS and allowlisted SSH Runtime targets without returning values or fingerprints to the UI.
            - Add local Personal Provider Packs for stdin-only custom CLI integrations.
            - Use the same redacted provider-push flow from supported AI coding agents.
            - Reuse a protected same-name value across registered projects without exposing it to the agent.
            - Resolve registered project aliases and search distinctive variable-name fragments using bounded, value-free metadata before asking for a folder path.
            - Push selected values to Expo EAS environments through the CLI hidden-value prompt without placing values in arguments, temporary files, or Kavranta output.

            ---

            Kavranta v__VERSION__ Windows 및 macOS 설치 파일입니다.

            - Env Manager에서 Kavranta로 이름을 바꾸면서 기존 앱 데이터와 프로젝트 manifest는 유지하고, 기존 AI 연결은 Kavranta 플러그인·Skill·MCP·Broker ID로 안전하게 전환합니다.
            - Windows 10/11(x64 베타, 미서명): `x64-setup.exe` 설치 파일. Microsoft Defender SmartScreen 경고가 나타날 수 있으므로 이 공식 릴리스에 첨부된 파일만 사용하세요.
            - Apple Silicon: aarch64 DMG
            - Intel Mac: x86_64 DMG
            - 앱은 영어를 기본으로 열리며 한국어도 지원합니다.
            - OpenCode의 전역 Skill·MCP·실패 시 차단되는 플러그인 인터페이스를 통해 다른 지원 에이전트와 같은 값 비노출 Broker 흐름을 사용합니다.
            - 검토한 Action Pack으로 고정 CLI 또는 HTTPS 대상을 실행하되 관리 값을 UI, 에이전트, 로그나 응답 본문에 반환하지 않습니다.
            - 구체적으로 요청한 값 없는 Action Pack을 지원 에이전트가 비공개 계획으로 설치하고, 별도 감사 작업으로 실행할 수 있습니다.
            - 관리 중인 Android App Links 인증서 지문을 로컬·원격 지문 원문을 에이전트에 반환하지 않고 지정한 공개 호스트와 검증할 수 있습니다.
            - env 파일명이 언급된 소스 코드 패치는 허용하면서 실제 패치 대상이 env 데이터 파일인 작업은 계속 차단합니다.
            - 생성값을 명령 인자나 에이전트 출력에 넣지 않고 5분·1회용 Broker 계획으로 바로 저장할 수 있습니다.
            - Wrangler `.dev.vars*`를 `.env*`와 함께 탐색하고 앱·설치된 AI 연동 업데이트를 조용히 백그라운드에서 확인합니다.
            - 마운트한 NAS나 기존 동기화 폴더에서 변경 불가능한 암호화 패키지를 주고받고, 충돌 검토 후 적용할 수 있습니다.
            - 선택한 값을 KMS 키 옵션과 함께 AWS Secrets Manager 또는 SSM Parameter Store로 전송할 수 있습니다.
            - 지원 AWS 대상과 허용된 SSH Runtime을 실제 값이나 해시를 UI에 반환하지 않고 비교할 수 있습니다.
            - 표준 입력 전용 사용자 CLI 연동을 Personal Provider Pack으로 추가할 수 있습니다.
            - 지원 AI 코딩 에이전트에서도 같은 값 비노출 Provider 전송 흐름을 사용할 수 있습니다.
            - 같은 이름의 보호된 값을 에이전트에 노출하지 않고 등록 프로젝트 사이에서 재사용할 수 있습니다.
            - 등록 프로젝트 별칭과 변수 이름 일부를 제한된 값 비노출 메타데이터로 검색해 이미 등록된 폴더 경로를 다시 묻지 않습니다.
            - 선택한 값을 명령 인자·임시 파일·Kavranta 출력에 넣지 않고 EAS CLI 숨김 입력으로 Expo EAS 환경에 전송할 수 있습니다.
          generateReleaseNotes: true
          prerelease: false
          releaseDraft: true
          retryAttempts: 2
          args: --target ${{ matrix.target }} --bundles app,dmg --config src-tauri/tauri.release.conf.json

      - name: Verify signed and notarized macOS bundles
        run: |
          bundle_root="target/${{ matrix.target }}/release/bundle"
          app_path="$(find "$bundle_root/macos" -maxdepth 1 -type d -name '*.app' -print -quit)"
          dmg_path="$(find "$bundle_root/dmg" -maxdepth 1 -type f -name '*.dmg' -print -quit)"
          if [[ -z "$app_path" || -z "$dmg_path" ]]; then
            echo "Expected macOS app and DMG bundles were not produced." >&2
            exit 1
          fi
          codesign --verify --deep --strict --verbose=2 "$app_path"
          codesign --verify --verbose=2 "$dmg_path"
          codesign --display --verbose=4 "$app_path" 2>&1 | grep -F "Authority=Developer ID Application:" >/dev/null
          spctl --assess --type execute --verbose=4 "$app_path"
          xcrun stapler validate "$app_path"

          mount_point="$RUNNER_TEMP/env-manager-${{ matrix.target }}-dmg"
          mkdir -p "$mount_point"
          hdiutil attach "$dmg_path" -readonly -nobrowse -mountpoint "$mount_point"
          trap 'hdiutil detach "$mount_point" >/dev/null 2>&1 || true' EXIT
          embedded_app="$(find "$mount_point" -maxdepth 1 -type d -name '*.app' -print -quit)"
          if [[ -z "$embedded_app" ]]; then
            echo "The DMG does not contain an app bundle." >&2
            exit 1
          fi
          codesign --verify --deep --strict --verbose=2 "$embedded_app"
          codesign --display --verbose=4 "$embedded_app" 2>&1 | grep -F "Authority=Developer ID Application:" >/dev/null
          spctl --assess --type execute --verbose=4 "$embedded_app"
          xcrun stapler validate "$embedded_app"
          if command -v syspolicy_check >/dev/null 2>&1; then
            syspolicy_check distribution "$embedded_app"
          fi

      - name: Attest macOS release artifacts
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: |
            target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            target/${{ matrix.target }}/release/bundle/macos/*.app.tar.gz

      - name: Remove temporary Apple signing material
        if: ${{ always() }}
        run: |
          if [[ -n "${ENV_MANAGER_SIGNING_KEYCHAIN:-}" ]]; then
            security delete-keychain "$ENV_MANAGER_SIGNING_KEYCHAIN" || true
          fi
          if [[ -n "${ENV_MANAGER_NOTARY_KEY_PATH:-}" ]]; then
            rm -f "$ENV_MANAGER_NOTARY_KEY_PATH"
          fi

  windows-beta:
    permissions:
      attestations: write
      contents: write
      id-token: write
    environment: release
    runs-on: windows-latest
    env:
      KAVRANTA_TARGET: x86_64-pc-windows-msvc
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable, 2026-08-23
        with:
          targets: x86_64-pc-windows-msvc
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          key: x86_64-pc-windows-msvc
      - run: npm ci
      - run: npm run validate:public-boundary
      - run: npm run validate:agent-bundle
      - run: npm run validate:release-version
      - run: npm run validate:release-distribution
      - run: npm run validate:provider-catalog

      - uses: tauri-apps/tauri-action@1deb371b0cd8bd54025b384f1cd735e725c4060f # action-v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: v__VERSION__
          releaseName: Kavranta v__VERSION__
          releaseBody: |
            Kavranta v__VERSION__ for macOS and Windows.

            - Renamed Env Manager to Kavranta while preserving existing app data and project manifests, and safely migrating legacy agent connections to the Kavranta plugin, Skill, MCP, and Broker identities.
            - Windows 10/11 (x64 beta, unsigned): download the `x64-setup.exe` installer. Microsoft Defender SmartScreen may warn; use only the installer attached to this official release.
            - Apple Silicon: download the aarch64 DMG
            - Intel Mac: download the x86_64 DMG
            - The app opens in English by default and also supports Korean.
            - Connect OpenCode through its global Skill, MCP, and fail-closed plugin interfaces, using the same redacted Broker workflow as the other supported agents.
            - Run locally reviewed Action Packs against a fixed CLI or HTTPS target without returning managed values to the UI, agent, logs, or response body.
            - Let a supported agent install a concretely requested, value-free Action Pack through a redacted plan, then run it as a separate audited action.
            - Verify managed Android App Links certificate fingerprints against explicit public hosts without returning local or remote fingerprints to the agent.
            - Allow source-code patches that only mention env filenames while continuing to block patches whose declared target is an env data file.
            - Pipe a generated value through a five-minute, single-use Broker plan without putting it in command arguments or agent output.
            - Discover Wrangler `.dev.vars*` alongside `.env*`, and surface app or installed AI-integration updates through quiet background checks.
            - Exchange immutable encrypted packages through a mounted NAS or existing sync folder, then review conflicts before applying.
            - Push selected values to AWS Secrets Manager or SSM Parameter Store with optional KMS keys.
            - Compare supported AWS and allowlisted SSH Runtime targets without returning values or fingerprints to the UI.
            - Add local Personal Provider Packs for stdin-only custom CLI integrations.
            - Use the same redacted provider-push flow from supported AI coding agents.
            - Reuse a protected same-name value across registered projects without exposing it to the agent.
            - Resolve registered project aliases and search distinctive variable-name fragments using bounded, value-free metadata before asking for a folder path.
            - Push selected values to Expo EAS environments through the CLI hidden-value prompt without placing values in arguments, temporary files, or Kavranta output.

            ---

            Kavranta v__VERSION__ Windows 및 macOS 설치 파일입니다.

            - Env Manager에서 Kavranta로 이름을 바꾸면서 기존 앱 데이터와 프로젝트 manifest는 유지하고, 기존 AI 연결은 Kavranta 플러그인·Skill·MCP·Broker ID로 안전하게 전환합니다.
            - Windows 10/11(x64 베타, 미서명): `x64-setup.exe` 설치 파일. Microsoft Defender SmartScreen 경고가 나타날 수 있으므로 이 공식 릴리스에 첨부된 파일만 사용하세요.
            - Apple Silicon: aarch64 DMG
            - Intel Mac: x86_64 DMG
            - 앱은 영어를 기본으로 열리며 한국어도 지원합니다.
            - OpenCode의 전역 Skill·MCP·실패 시 차단되는 플러그인 인터페이스를 통해 다른 지원 에이전트와 같은 값 비노출 Broker 흐름을 사용합니다.
            - 검토한 Action Pack으로 고정 CLI 또는 HTTPS 대상을 실행하되 관리 값을 UI, 에이전트, 로그나 응답 본문에 반환하지 않습니다.
            - 구체적으로 요청한 값 없는 Action Pack을 지원 에이전트가 비공개 계획으로 설치하고, 별도 감사 작업으로 실행할 수 있습니다.
            - 관리 중인 Android App Links 인증서 지문을 로컬·원격 지문 원문을 에이전트에 반환하지 않고 지정한 공개 호스트와 검증할 수 있습니다.
            - env 파일명이 언급된 소스 코드 패치는 허용하면서 실제 패치 대상이 env 데이터 파일인 작업은 계속 차단합니다.
            - 생성값을 명령 인자나 에이전트 출력에 넣지 않고 5분·1회용 Broker 계획으로 바로 저장할 수 있습니다.
            - Wrangler `.dev.vars*`를 `.env*`와 함께 탐색하고 앱·설치된 AI 연동 업데이트를 조용히 백그라운드에서 확인합니다.
            - 마운트한 NAS나 기존 동기화 폴더에서 변경 불가능한 암호화 패키지를 주고받고, 충돌 검토 후 적용할 수 있습니다.
            - 선택한 값을 KMS 키 옵션과 함께 AWS Secrets Manager 또는 SSM Parameter Store로 전송할 수 있습니다.
            - 지원 AWS 대상과 허용된 SSH Runtime을 실제 값이나 해시를 UI에 반환하지 않고 비교할 수 있습니다.
            - 표준 입력 전용 사용자 CLI 연동을 Personal Provider Pack으로 추가할 수 있습니다.
            - 지원 AI 코딩 에이전트에서도 같은 값 비노출 Provider 전송 흐름을 사용할 수 있습니다.
            - 같은 이름의 보호된 값을 에이전트에 노출하지 않고 등록 프로젝트 사이에서 재사용할 수 있습니다.
            - 등록 프로젝트 별칭과 변수 이름 일부를 제한된 값 비노출 메타데이터로 검색해 이미 등록된 폴더 경로를 다시 묻지 않습니다.
            - 선택한 값을 명령 인자·임시 파일·Kavranta 출력에 넣지 않고 EAS CLI 숨김 입력으로 Expo EAS 환경에 전송할 수 있습니다.
          generateReleaseNotes: true
          prerelease: false
          releaseDraft: true
          retryAttempts: 2
          updaterJsonPreferNsis: true
          args: --target x86_64-pc-windows-msvc --bundles nsis --config src-tauri/tauri.windows-beta.conf.json

      - name: Verify explicitly unsigned Windows beta
        shell: pwsh
        run: |
          $files = @(
            Get-ChildItem "target/x86_64-pc-windows-msvc/release/env-manager-app.exe" -ErrorAction Stop
            Get-ChildItem "target/x86_64-pc-windows-msvc/release/bundle/nsis/*-setup.exe" -ErrorAction Stop
          )
          foreach ($file in $files) {
            $signature = Get-AuthenticodeSignature -FilePath $file.FullName
            if ($signature.Status -ne "NotSigned") {
              throw "Windows beta signing state changed for $($file.Name): $($signature.Status). Update the release policy and documentation before publishing."
            }
          }

      - name: Attest Windows release artifacts
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: target/x86_64-pc-windows-msvc/release/bundle/nsis/*-setup.exe

  publish-release:
    needs: [macos, windows-beta]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Publish verified release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          release_tag="v$(jq -r '.version' package.json)"
          gh release edit "$release_tag" --notes-file RELEASE_NOTES.md --draft=false --latest
